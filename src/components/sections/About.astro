---
title: "My About"
description: "This is the about file"

import Section from "../Section.astro"

import { basics } from "@cv"
const { paragraphs } = basics
---

<Section title="Resumen profesional">
  {paragraphs.map((paragraph: string) => (
    <p set:html={paragraph} />
  ))}
</Section>

<style>
  p {
    margin-bottom: 16px;
    color: #444;
    font-size: 0.9rem;
    line-height: 1.6;
  }

  p:last-child {
    margin-bottom: 0;
  }

  :global(p strong) {
    font-weight: 600;
    color: #222;
  }
</style>
